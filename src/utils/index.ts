import { Base } from './Base';
import * as Const from './Const';
import { Logger } from './Logger';
import { Parser } from './Parser';

const Utils = {
    const: Const,
    base: Base,
    logger: Logger,
    parser: Parser
};

export default Utils;
export { Const, Base, Logger, Parser };
